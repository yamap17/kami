# codegen.yml
schema: http://localhost:3033/graphql
overwrite: true
documents: graphql/**/*.graphql
generates:
  graphql/csrApi.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-urql